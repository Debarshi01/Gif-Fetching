// import App from "./App";
import FavGif from "./Components/FavGif";
function  FavPage({favgif , onDelete, onClick}){
    // return <FavGif favgif={favgif} onClick={onClick} onDelete={onDelete}/>
    return <div>
        Hello , this is the redirected page;
    </div>
}

export default FavPage;
